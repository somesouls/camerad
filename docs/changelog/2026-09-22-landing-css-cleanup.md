# Landing CSS cleanup

- Consolidate theme variables into one canonical dark `:root` and one explicit light-theme override.
- Keep the agreed earth-tone palette as the only active color system.
- Merge repeated selectors and declarations while preserving responsive behavior.
- Replace variable/system display stacks and tiny monospace labels with a clear Arial-based sans-serif stack.
- Disable ligatures and normalize small-label sizes and font weights for better readability.
- Validate the landing page before adapting the design to `login.html`.

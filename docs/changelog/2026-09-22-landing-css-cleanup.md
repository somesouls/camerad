# Landing CSS cleanup

- Consolidate theme variables into one canonical dark `:root` and one explicit light-theme override.
- Keep the agreed earth-tone palette as the only active color system.
- Merge repeated selectors, responsive media blocks, and duplicate declarations while preserving behavior.
- Replace variable/system display stacks and tiny monospace labels with a clear Arial/Helvetica sans-serif stack.
- Define the same readable stack for body, display headings, and small labels.
- Disable ligatures, normalize font weights, and keep microcopy at a readable minimum size.
- Remove the retired purple/cyan tokens and temporary migration workflows.
- Validate the landing page before adapting the design to `login.html`.

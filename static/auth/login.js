(()=>{'use strict';const params=new URLSearchParams(location.search),next=params.get('next')||'/',f=document.getElementById('f'),e=document.getElementById('e'),btn=document.getElementById('btn');f.addEventListener('submit',async ev=>{ev.preventDefault();e.style.display='none';btn.disabled=true;btn.textContent='Memeriksa…';try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.getElementById('u').value,password:document.getElementById('p').value})}),j=await r.json();if(r.ok&&j.ok){location.href=next;return}e.textContent=j.error||'Nama pengguna atau kata sandi tidak sesuai.';e.style.display='block'}catch{e.textContent='Tidak dapat terhubung ke server. Coba kembali.';e.style.display='block'}finally{btn.disabled=false;btn.textContent='Masuk'}})})();